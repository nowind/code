c32asm 是一个功能很强大的静态反汇编工具
他 支持 导入 导出 表查看，字符串查看（多字节），jmp指令计算，16进制修改
特点 ：
对32位pe程序分析很不错，字符串分析，call，jmp等指令分析也很到位

缺点：
unicode 字符串支持差，crt函数无法解析，socket函数解析差（虽然可以导入符号表，但是是ini格式的）
插件功能不够强大，插件不是很多

是W32dsm 很好的替代品是ida很好的补充

去 看雪论坛下载